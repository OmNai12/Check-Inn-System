<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.5.xsd">

    <changeSet id="004" author="yourname">
        <createTable tableName="payment">
            <column name="payment_id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="amount" type="DECIMAL(10,2)">
                <constraints nullable="false"/>
            </column>
            <column name="payment_method" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="payment_status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="payment_date" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="reservation_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Foreign Key Constraint must be OUTSIDE the createTable block -->
        <addForeignKeyConstraint 
            baseTableName="payment" baseColumnNames="reservation_id"
            referencedTableName="reservations" referencedColumnNames="reservation_id"
            constraintName="fk_payment_reservation"/>
    </changeSet>

</databaseChangeLog>
